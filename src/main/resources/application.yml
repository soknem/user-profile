spring:
  application:
    name: user-profile-service
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://35.193.215.248:8888
      username: user
      password: password
    stream:
      kafka:
        binder:
          brokers: 35.193.215.248:50008
    bus:
      trace:
        enabled: true
      refresh:
        enabled: true
      env:
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always
    refresh:
      access: read_only

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG

